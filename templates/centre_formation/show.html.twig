{% extends 'base.html.twig' %}

{% block title %}CentreFormation{% endblock %}

{% block body %}
    <h1>CentreFormation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre_formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ centre_formation.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ centre_formation.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_centre_formation_index') }}">back to list</a>

    <a href="{{ path('app_centre_formation_edit', {'id': centre_formation.id}) }}">edit</a>

    {{ include('centre_formation/_delete_form.html.twig') }}
{% endblock %}
