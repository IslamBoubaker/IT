{% extends 'base.html.twig' %}

{% block title %}MonitoringSession{% endblock %}

{% block body %}
    <h1>MonitoringSession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monitoring_session.id }}</td>
            </tr>
            <tr>
                <th>EtatSalle</th>
                <td>{{ monitoring_session.etatSalle }}</td>
            </tr>
            <tr>
                <th>EtatSupports</th>
                <td>{{ monitoring_session.etatSupports }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_monitoring_session_index') }}">back to list</a>

    <a href="{{ path('app_monitoring_session_edit', {'id': monitoring_session.id}) }}">edit</a>

    {{ include('monitoring_session/_delete_form.html.twig') }}
{% endblock %}
